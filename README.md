# firesex

Firefox config - zero bullshit. Telemetry's nuked, shady features are gone too.
user.js calls the shots, userChrome.css scrubs the UI clean. Nothing else touched.
Runs on the latest version across Windows, macOS, and Linux.
